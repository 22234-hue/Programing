import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import ts from 'typescript';
const source=fs.readFileSync(new URL('../lib/media/validation.ts',import.meta.url),'utf8'),code=ts.transpileModule(source,{compilerOptions:{module:ts.ModuleKind.ESNext,target:ts.ScriptTarget.ES2022}}).outputText,{normalizeType,matchesSignature,parseRange}=await import('data:text/javascript;base64,'+Buffer.from(code).toString('base64'));
test('accepts the media formats used by photo and voice messages',()=>{assert.equal(normalizeType('audio/webm;codecs=opus'),'audio/webm');assert.equal(normalizeType('','voice.m4a'),'audio/mp4');assert.equal(matchesSignature('image/png',new Uint8Array([137,80,78,71,13,10,26,10])),true);assert.equal(matchesSignature('audio/webm',new Uint8Array([26,69,223,163])),true);assert.equal(matchesSignature('image/png',new TextEncoder().encode('not png')),false);});
test('parses normal and suffix ranges safely',()=>{assert.deepEqual(parseRange('bytes=0-9',100),{offset:0,length:10});assert.deepEqual(parseRange('bytes=-5',100),{offset:95,length:5});assert.equal(parseRange('bytes=100-200',100),null);assert.equal(parseRange('bytes=0-1,4-5',100),null);});
